{
  // "ServiceAddressUri": "http://mylittleurlapi:80/",     // When the whole setup (App, API & mongodb) is running in docker 
  "ServiceAddressUri": "http://localhost:32780/",    // When debugging this app on localhost (API & mongodb in docker)
  // "ServiceAddressUri": "https://localhost:5001/",    // Debugging API on localhost https (testing Azure storage)
  // "ServiceAddressUri": "https://mylittleurlapi.azurewebsites.net/", // Azure hosted API and storage

  "BaseAddressPrefix": "http://mylittleurl.us/",         // Public site address for prefix
  "Logging": {
    "IncludeScopes": false,
    "LogLevel": {
      "Default": "Warning"
    }
  }
}
