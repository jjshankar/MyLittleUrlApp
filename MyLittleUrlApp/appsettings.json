{
  //"ServiceAddressUri": "http://mylittleurlapi:80/",   // docker (mongodb)
  // "ServiceAddressUri": "http://localhost:32780/",    // localhost (mongodb)
  "ServiceAddressUri": "https://localhost:5001/",       // localhost https (Azure storage)
  "Logging": {
    "IncludeScopes": false,
    "LogLevel": {
      "Default": "Warning"
    }
  }
}
